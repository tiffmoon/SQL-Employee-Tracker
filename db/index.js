// all of the methods that are universal 

// const db = require db

// add employee

// remove emplyee etc

const db = require("./connection");

class Database {
  constructor(db) {
    this.db = db
  }
}